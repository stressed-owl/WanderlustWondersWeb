import React from "react";
import CustomCardContent from "./CustomCardContent";

const StyledCustomCardContent = ({ children }) => {
  return <CustomCardContent>{children}</CustomCardContent>;
};

export default StyledCustomCardContent;